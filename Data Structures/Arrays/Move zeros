#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin>>t;
    for(int j=0;j<t;j++){
        int n;
        cin>>n;
        int a[n];
        vector<int> a1;
        for(int i=0;i<n;i++){
            cin>>a[i];
            if(a[i]!=0){
                a1.push_back(a[i]);
            }
        }
        for(int i=a1.size();i<n;i++){
        
            a1.push_back(0);
        }
        for(int i=0;i<n;i++){
            cout<<a1[i]<<" ";
        }
        cout<<endl;
        
    }
	
	return 0;
}
